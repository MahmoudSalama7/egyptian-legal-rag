[project]
name = "egyptian-legal-rag"
version = "0.1.0"
description = "Egyptian Legal RAG System"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pymupdf>=1.26.0",
    "pytest>=8.0.0",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "dvc>=3.67.1",
]
